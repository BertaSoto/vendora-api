{
  "framework": "nextjs",
  "buildCommand": "corepack pnpm run build",
  "outputDirectory": ".next",
  "installCommand": "corepack enable && corepack prepare pnpm@9.15.9 --activate && corepack pnpm --version && corepack pnpm install --frozen-lockfile"
}
